//Find if there is a subarray with 0 sum
//Geeks - geeksforgeeks.org/find-if-there-is-a-subarray-with-0-sum/
#include <bits/stdc++.h>
using namespace std;
const int N = 1e5;
int a[N];
int p[N]; 
int main()
{
    int n,i;
    cin >> n;
    for(int i=0;i<n;i++)
        cin >> a[i];
    /
    /
    /
    /
    
}